apiVersion: v1
kind: Service
metdaata:
  name: redis-service
  labels:
    name: redis-service
    app: demo-voting-app
spec:
  ports:
    - Port: 6379
      targetPort: 6379
  selector:
    name: redis-pod
    app: demo-voting-app
